Object
	 `Item /** Oggetto generico che puo` essere aggiunto all'inventario **/
		| /* Contiene dati sull'oggetto che verra` poi aggiunto, setta quei dati sull'oggetto quando lo si equipaggia */
		|`Weapon /** Arma generica **/ /* contiene info su: resistenza, effetti applicati sull'arma */
		|`Armour /** Armatura generica **/ /* contiene info su: resistenza, effetti applicati sull'armatura */
		|`Consumable /** Oggetto consumabile generico **/
		|`Ammunation /** Munizioni **/ /* Usate ogni volta che si spara, hanno una categoria di armi (enum?) associata (possono essere usate solo per quel tipo di armi). Contengono info su danno ed eventuali effetti applicati OnHit() */
		 `Key /** Chiave **/ /* permette di aprire le porte che richiedono tale chiave */

Actor
	|`InteractableActor /** Oggetto con cui si puo` interagire **/
	|	|`PickupActor /** Oggetto che puo` essere raccolto. Aggiunge `Item` all'inventario **/
	|	 `DoorActor /** Porta: si puo` aprire, chiudere e puo` avere una chiave **/
	|
	|`EquippableActor /** Oggetto che puo` essere equipaggiato **/
		|`EquippableWeapon /** Arma generica **/
		|	|`EquipableMeleeWeapon /** Arma generica corpo a corpo **/
		|	|	`/* [Una classe per ogni tipo di arma] */
		|	 `EquipableRangedWeapon /** Arma generica a distanza **/
		|		`/* [Una classe per ogni tipo di arma] */
		|`EquipableArmour /** Armatura generica (include vestiti) **/
		 `EquipableConsumable /** Oggetto generico che puo` essere utilizzato una sola volta **/
			|`EquipablePotion /** Pozione generica **/
			|	 `/* [Una classe per ogni tipo di pozione] */
			 `/* [Cibo?] */

Pawn
	 `Character /** Personaggio generico **/
		|`PlayerCharacter /** Personaggio controllato da un giocatore **/
			 `/* TODO tutti i personaggi controllati dall'AI */